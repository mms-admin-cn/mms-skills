# Cursor 引导（mms-skills）

本项目使用共享技能库：`/Volumes/SXPCWLKJ/MyWork/mms-skills/skills-shared`。

默认不加载技能。先按任务复杂度分级，再决定是否读取 `SKILL.md`：

- Level 0：问候、确认、谢谢、改颜色、修拼写、一行配置等，直接回复或直接改，不读技能。
- Level 1：单一 bug、小字段、小样式、小组件，快速分析后实施；必要时只读一个相关技能。
- Level 2：新增页面、新功能、多文件或跨模块变更，读取 `mms-dev-workflow/SKILL.md`，先分析，再建 `version/vX.Y.Z-功能说明.md`，再开发验证。
- Level 3：新系统、架构变更、从零搭建，读取 `brainstorming/SKILL.md`，再按需进入计划和执行流程。

用户说“快速改”“直接改”“just do it”“不用走流程”时，立即跳过流程技能。

全部中文回答（代码、命令、日志原文除外）。涉及服务器写操作、生产发布、数据库写入、删除文件、真实密钥等外部状态变更时，先说明影响并等待明确确认。
