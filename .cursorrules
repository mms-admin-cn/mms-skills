本仓库采用共享技能库模式。

共享技能目录：`/Volumes/SXPCWLKJ/MyWork/mms-skills/skills-shared`

处理任务时：
1. 先定位目标技能目录
2. 先读取 `SKILL.md`
3. 按 `mms-dev-workflow` 执行：分析 -> version 文档 -> 开发 -> 进度同步 -> 复盘沉淀
4. 若任务要求与技能冲突，以任务显式要求优先

强制规范：
- 全部中文回答（代码/命令/日志原文除外）
- 纯问候/状态确认直接回复，不加载技能
- 新功能必须先分析，再在项目根目录 `version/` 创建带版本号文档
- 版本号必须递增
- 开发过程持续同步任务进度
- 完成后做自我进化：学习总结并沉淀到 skill

维护约定：
- 技能只维护在 `skills-shared/`
- 一个技能一个目录，最少含 `SKILL.md`